# Squall Parallel

Squall is an efficient parallel execution library for Ruby 2.0+.

## Requirements

- Ruby >= 2.0.0
- Rails >= 4.0 (optional)

## Features

- Thread and Process executors.
- Support for Queue and Enumerable::Lazy collections.
- ActiveRecord connection pool handling for Rails 4.
